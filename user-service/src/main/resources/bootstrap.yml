spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true   # Stops service if Config Server is unavailable
  profiles:
    active: default     # Ensures it loads the default profile
